@model IEnumerable<GanaderiaLasDelicias.Models.Pregnancy>

@{
    ViewData["Title"] = "Index";
}

<p>
    <a asp-action="Create" class="btn btn-block bg-gradient-primary">Registrar Embarazo</a>
</p>
<div class="card">
    <div class="card-header">
        <h3 class="card-title">Lista de Embarazos</h3>
        <div class="card-tools">
            <div class="input-group input-group-sm" style="width: 150px;">
                <input type="text" name="table_search" class="form-control float-right" placeholder="Search">
                <div class="input-group-append">
                    <button type="submit" class="btn btn-default">
                        <i class="fas fa-search"></i>
                    </button>
                </div>
            </div>
        </div>
    </div>
    <div style="card-body; table-responsive p-0">
        <table class="table table-hover text-nowrap">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PregDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Note)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Bull)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cow)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PregDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Note)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Bull.BullId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cow.CowId)
            </td>
                        <td>
                            <div class="btn-group">
                                <a asp-action="Edit" asp-route-id="@item.PregnancyId" class="btn btn-success">Editar</a>
                                <a asp-action="Details" asp-route-id="@item.PregnancyId" class="btn btn-info">Detalles</a>
                                <a asp-action="Delete" asp-route-id="@item.PregnancyId" class="btn btn-danger">Desactivar</a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
